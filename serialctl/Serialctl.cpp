// Arduino CMake requires a different syntax for header-only libraries
// So instead of having a header-only library we put a blank cpp file for it to find
// If we ever end up with stuff that can go outside of a header file we can remove this.